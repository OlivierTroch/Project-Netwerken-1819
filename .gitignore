i## .gitignore -- Voeg in dit bestand de namen toe van bestanden die niet in
## het versiebeheersysteem thuis horen. Tekstpatronen (*, ?, ...) kunnen ook.

# Editor backup files
.*~
*~
*.bak

# Virtual machine files
*.iso
*.box
*.ova
*.vmdk

# Hidden Vagrant-directory
.vagrant

#txt file waar ik(Robin) tasks van huidige week bijhou
weekrapport/tasks_weekrapport.txt

#vagrantfile ignoren, mijn boxfile staat in C:\boxfiles/vagrant-centos76.box
Vagrantfile
vagrant-hosts.yml
SmartStore
resources/*